<h2>id: <%= @c_board.id %> のメッセージ編集ページ</h2>

<%= form_for(@c_board) do |f| %>

  <%= f.label :title, 'タイトル' %>
  <%= f.text_field :title %>
  
  <%= f.label :content, '詳細' %>
  <%= f.text_field :content %>

  <%= f.submit '投稿' %>
<% end %>

<%= link_to '一覧に戻る', c_boards_path %>